import React from 'react';
import { Button } from 'reactstrap';

const Btn = (props) => {
  return (
    <div>
      <Button outline color="primary">Create</Button>{' '}
    </div>
  );

}

export default Btn;